```bash

git checkout -b feature/basic-login-page
git add index.html styles.css
git commit -m "Iteration 1: Basic Login Page"
git push --set-upstream origin feature/basic-login-page




git checkout -b feature/state-management
# Make changes to implement state management
git add .
git commit -m "Iteration 2: Adding State Management"
git push -u origin feature/state-management




npm install mssql
npm install next-auth --legacy-peer-deps


```
